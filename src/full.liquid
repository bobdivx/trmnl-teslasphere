{% comment %}TeslaSphere TRMNL — batterie, charge, ville, Sentinel{% endcomment %}
{% assign battery_level = battery_level | default: usable_battery_level | default: 0 | plus: 0 %}
{% if battery_level > 100 %}{% assign battery_level = 100 %}{% endif %}
{% if battery_level < 0 %}{% assign battery_level = 0 %}{% endif %}
{% assign charge_limit = charge_limit_soc | default: 80 | plus: 0 %}
{% assign vehicle_name = display_name | default: "Tesla" %}
{% assign charge_text = charging_label | default: charging_state | default: "Inconnu" %}
{% assign city_text = city | default: "—" %}
{% assign sentry_text = sentry_label | default: "—" %}
{% assign is_fresh = data_fresh %}
{% if is_fresh == true or is_fresh == "true" %}
  {% assign freshness_label = "Données live" %}
{% else %}
  {% assign freshness_label = "Données en cache" %}
{% endif %}

<div class="layout layout--col gap--large">
  <div class="flex flex--col gap--small">
    <span class="label label--small text--gray-40">Batterie</span>
    <div class="flex flex--row flex--cross-end gap--small">
      <span class="value value--xxlarge value--tnums lg:value--xxxlarge">{{ battery_level }}</span>
      <span class="label label--large text--gray-40">%</span>
    </div>
    <div class="progress-bar progress-bar--large">
      <div class="content">
        <span class="label">Limite {{ charge_limit }}%</span>
        <span class="value value--xxsmall value--tnums">{{ battery_level }}%</span>
      </div>
      <div class="track">
        <div class="fill" style="width: {{ battery_level }}%"></div>
      </div>
    </div>
  </div>

  <div class="grid grid--cols-1 gap--medium">
    <div class="item gap--xsmall">
      <span class="label label--xsmall text--gray-40">Charge</span>
      <span class="value value--large" data-clamp="1">{{ charge_text }}</span>
    </div>
    <div class="item gap--xsmall">
      <span class="label label--xsmall text--gray-40">Emplacement</span>
      <span class="value value--large" data-clamp="1">{{ city_text }}</span>
    </div>
    <div class="item gap--xsmall">
      <span class="label label--xsmall text--gray-40">Mode Sentinel</span>
      <span class="value value--large" data-clamp="1">{{ sentry_text }}</span>
    </div>
  </div>

  <span class="label label--xsmall text--gray-40">{{ freshness_label }}</span>
</div>
<div class="title_bar">
  <span class="title">TeslaSphere</span>
  <span class="instance">{{ vehicle_name }}</span>
</div>
